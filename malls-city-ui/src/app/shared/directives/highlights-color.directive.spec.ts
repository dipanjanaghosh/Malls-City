import { HighlightsColorDirective } from './highlights-color.directive';

describe('HighlightsColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightsColorDirective();
    expect(directive).toBeTruthy();
  });
});
